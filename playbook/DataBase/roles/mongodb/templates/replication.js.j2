use admin
db.createUser({user:"{{ admin_user }}",pwd:"{{ admin_pass }}",roles:[{role:"__system",db:"admin"}]})
use admin
db.auth("{{ admin_user }}","{{ admin_pass }}")
{% for i in groups['slave']%}
rs.add("{{ i }}:{{ mongo_port }}")
{% endfor %}
